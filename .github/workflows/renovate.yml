---
name: Renovate
on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

jobs:
  renovate:
    uses: shoogland/.github/.github/workflows/renovate.yml@main
    secrets:
      SH_UPDATER_APP_PRIVATE_KEY: ${{ secrets.SH_UPDATER_APP_PRIVATE_KEY }}
